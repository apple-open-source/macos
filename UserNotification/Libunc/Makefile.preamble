PUBLIC_HEADER_DIR = /usr/local/include
LIBRARY_STYLE = STATIC
STRIP_ON_INSTALL = NO
BEFORE_INSTALL += debug profile

# for building 64-bit
# <rdar://problem/3820837> libunc needs to build with gcc-3.5 and 3-way fat
export CCOMPILER = /usr/bin/gcc-3.5
NEXTSTEP_OBJCPLUS_COMPILER = $(CCOMPILER)
