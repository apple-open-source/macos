This change modifies a header file transitively included by `stdlib.h`.  These headers are extremely sensitive, as they are included extremely broadly throughout both internal and external code.  Confirm whether a Side World Build is necessary to qualify this change, and run one if so.
