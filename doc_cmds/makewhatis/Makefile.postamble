include $(CoreOSMakefiles)/ProjectBuilder/Makefile.Postamble.Common

after_install::
	$(INSTALL_DIRECTORY) $(DSTROOT)$(MANDIR)
	$(INSTALL_PROGRAM) -c makewhatis.local $(DSTROOT)$(NEXTSTEP_INSTALLDIR)
