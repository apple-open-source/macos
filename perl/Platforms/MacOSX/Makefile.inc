DEFAULT = 5.28
KNOWNVERSIONS = 5.18 5.28
BOOTSTRAPPERL = 5.30
EXTRAVERSIONERFLAGS = -DFORCE_TWO_NUMBER_VERSIONS

MY_ARCHS := $(or $(RC_ARCHS),$(MY_HOST))
ARCHFLAGS := $(foreach A,$(MY_ARCHS),-arch $(A))
EXTRAMERGE = mergeplist mergeman
MERGEDEFAULT = usr/local/OpenSourceLicenses
