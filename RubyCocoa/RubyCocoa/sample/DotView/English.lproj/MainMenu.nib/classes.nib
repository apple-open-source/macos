{
    IBClasses = (
        {
            ACTIONS = {setColor = id; setRadius = id; }; 
            CLASS = DotView; 
            LANGUAGE = ObjC; 
            OUTLETS = {colorWell = id; sizeSlider = id; }; 
            SUPERCLASS = NSView; 
        }, 
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }
    ); 
    IBVersion = 1; 
}