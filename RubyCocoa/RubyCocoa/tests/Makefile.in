TARGET = objc_test.bundle
OBJS = objc_test.o
CFLAGS = %%%other_cflags%%%

${TARGET}: ${OBJS}
	cc -o $@ ${CFLAGS} -bundle -framework Foundation -framework AddressBook ${OBJS}

clean:
	rm -f ${TARGET} ${OBJS}
