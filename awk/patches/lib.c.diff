--- lib.c.orig	2008-11-12 11:50:29.000000000 -0800
+++ lib.c	2008-11-12 11:41:52.000000000 -0800
@@ -155,6 +155,8 @@
 			}
 			setfval(nrloc, nrloc->fval+1);
 			setfval(fnrloc, fnrloc->fval+1);
+			if (donefld == 0)
+		                fldbld();
 			*pbuf = buf;
 			*pbufsize = bufsize;
 			return 1;
