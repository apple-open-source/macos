--- lib.c.orig	2005-03-16 17:14:14.000000000 -0800
+++ lib.c	2005-03-22 17:47:51.000000000 -0800
@@ -154,6 +154,8 @@
 			}
 			setfval(nrloc, nrloc->fval+1);
 			setfval(fnrloc, fnrloc->fval+1);
+			if (donefld == 0)
+		                fldbld();
 			*pbuf = buf;
 			*pbufsize = bufsize;
 			return 1;
