#! /bin/sh

# Error if stamp-h appears in AC_OUTPUT line.

. $srcdir/defs || exit 1

cat > configure.in << 'END'
AC_OUTPUT(Makefile stamp-h)
END

: > Makefile.am

$ACLOCAL || exit 1
$AUTOMAKE && exit 1
exit 0
