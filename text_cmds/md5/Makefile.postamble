include $(CoreOSMakefiles)/ProjectBuilder/Makefile.Postamble.Common

#LOCAL_CFLAGS = -I/usr/local/include/openssl

after_install::
	mkdir -p $(DSTROOT)/usr/share/man/man1
	install -c -m 444 md5.1 $(DSTROOT)/usr/share/man/man1
