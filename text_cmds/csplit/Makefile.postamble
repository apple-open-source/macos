include $(CoreOSMakefiles)/ProjectBuilder/Makefile.Postamble.Common

after_install:
	mkdir -p "$(DSTROOT)/usr/share/man/man1"
	install -c -m 644 -o root -g wheel csplit.1 "$(DSTROOT)/usr/share/man/man1/csplit.1"
