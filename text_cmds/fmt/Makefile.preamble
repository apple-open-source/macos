vpath def.h       ../mail
vpath extern.h    ../mail
vpath pathnames.h ../mail
vpath rcv.h       ../mail
vpath head.c      ../mail

HFILES += def.h extern.h pathnames.h rcv.h
CFILES += head.c

include $(CoreOSMakefiles)/ProjectBuilder/Makefile.Preamble.Common
