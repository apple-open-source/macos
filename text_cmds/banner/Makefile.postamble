include $(CoreOSMakefiles)/ProjectBuilder/Makefile.Postamble.Common
after_install:
	mkdir -p "$(DSTROOT)/usr/share/man/man1"
	mkdir -p "$(DSTROOT)/usr/share/man/man6"
	install -c -m 644 banner.1 "$(DSTROOT)/usr/share/man/man1/banner.1"
	install -c -m 644 banner.6 "$(DSTROOT)/usr/share/man/man1/banner.6"
