--- Makefile.in.orig	2008-09-18 18:20:48.000000000 -0700
+++ Makefile.in	2008-09-18 18:21:00.000000000 -0700
@@ -186,7 +186,7 @@
 top_srcdir = @top_srcdir@
 EXTRA_DIST = MAINT
 SUBDIRS = src magic tests doc python
-all: config.h
+all:
 	$(MAKE) $(AM_MAKEFLAGS) all-recursive
 
 .SUFFIXES:
@@ -206,7 +206,7 @@
 	cd $(top_srcdir) && \
 	  $(AUTOMAKE) --gnu  Makefile
 .PRECIOUS: Makefile
-Makefile: $(srcdir)/Makefile.in $(top_builddir)/config.status
+Makefile:
 	@case '$?' in \
 	  *config.status*) \
 	    echo ' $(SHELL) ./config.status'; \
