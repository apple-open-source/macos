--- file-4.10/src/magic.c	Sat Jul 24 15:55:17 2004
+++ src/magic.c	Wed Sep  1 22:56:17 2004
@@ -313,6 +313,9 @@
 			file_tryelf(ms, fd, buf, (size_t)nbytes);
 		}
 #endif
+#ifdef BUILTIN_MACHO
+		file_trymacho(ms, fd, buf, (size_t)nbytes, inname);
+#endif /* BUILTIN_MACHO */
 	}
 	rv = 0;
 done:
