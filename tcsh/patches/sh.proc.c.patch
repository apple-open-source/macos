diff -up /SourceCache/tcsh/tcsh-58/tcsh/sh.proc.c ./sh.proc.c
--- /SourceCache/tcsh/tcsh-58/tcsh/sh.proc.c	2005-03-03 11:57:07.000000000 -0800
+++ sh.proc.c	2007-05-01 23:22:24.000000000 -0700
@@ -603,8 +603,10 @@ pjwait(pp)
         (void) signal(SIGINT, inthandler);
 #endif /* UNRELSIGS */
 #ifdef BSDJOBS
-    if (tpgrp > 0)		/* get tty back */
+    if (tpgrp > 0) {		/* get tty back */
+	    signal(SIGTTOU, SIG_IGN);
 	(void) tcsetpgrp(FSHTTY, tpgrp);
+    }
 #endif /* BSDJOBS */
     if ((jobflags & (PSIGNALED | PSTOPPED | PTIME)) ||
 	!eq(dcwd->di_name, fp->p_cwd->di_name)) {
