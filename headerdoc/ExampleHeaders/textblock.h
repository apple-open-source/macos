/*! @header
    @discussion

	This is an example of how to use a textblock element.

	This is also an example of how to use a textblock element.

	Spaces with pre and textblock on separate lines:

        <pre>
        @textblock
        if (a<3) {
                printf("a is @ less @than 3.\n");
        }
        @/textblock
        </pre>

	Spaces with pre and textblock on the same line:

        <pre>@textblock
        if (a<3) {
                printf("a is @ less @than 3.\n");
        }
        @/textblock
        </pre>

	Tabs with pre and textblock on separate lines:

	<pre>
	@textblock
	if (a<3) {
		printf("a is @ less @than 3.\n");
	}
	@/textblock
	</pre>

	Tabs with pre and textblock on the same line:

	<pre>@textblock
	if (a<3) {
		printf("a is @ less @than 3.\n");
	}
	@/textblock
	</pre>

	@textblock i<3 @/textblock

	For more information, reread this page.
 */

