STRIPFLAGS=
after_install:
	mkdir -p "$(DSTROOT)/usr/share/man/man1"
	mkdir -p "$(DSTROOT)/usr/share/man/man5"
	install -c -m 644 uuencode.1 "$(DSTROOT)/usr/share/man/man1/uuencode.1"
	install -c -m 644 uuencode.5 "$(DSTROOT)/usr/share/man/man5/uuencode.5"
